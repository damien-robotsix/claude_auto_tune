# Claude local state (persisted via Docker volumes)
.claude-home/
.claude/settings.local.json

# Environment and secrets
.env
.env.*
*.key
*.pem

# Agent scratch space (per CLAUDE.md CI sandbox rules)
.scratch/
